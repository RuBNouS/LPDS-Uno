<Window x:Class="Uno.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Uno.ViewModels"
        xmlns:views="clr-namespace:Uno.Views"
        mc:Ignorable="d"
        Title="UNO Desktop" Height="720" Width="1080" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:LobbyViewModel}">
            <views:LobbyView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:TabuleiroViewModel}">
            <views:TabuleiroView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ResultadosViewModel}">
            <views:ResultadosView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:RegrasViewModel}">
            <views:RegrasView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SavesViewModel}">
            <views:SavesView/>
        </DataTemplate>
    </Window.Resources>

    <ContentControl Content="{Binding CurrentViewModel}" />
</Window>